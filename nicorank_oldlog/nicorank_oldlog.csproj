<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Costura.Fody" Version="5.7.0">
      <PrivateAssets>all</PrivateAssets>
    </PackageReference>
    <PackageReference Include="Fody" Version="6.8.1">
      <PrivateAssets>all</PrivateAssets>
    </PackageReference>
    <PackageReference Include="Newtonsoft.Json" Version="13.0.3" />
    <PackageReference Include="System.Text.Encoding.CodePages" Version="8.0.0" />
    <!-- nicorankLibはpackages.config形式のためSQLiteのDLLがtransitiveに流れてこない。
         週刊の動画情報キャッシュ保存（SaveApiXmlCache）でSQLiteを使うため、CLIと同一の実証済み方式で直接参照する。
         バージョンはnicorankLibと同一の10.0.11（SQLitePCLRaw 2.1.12系）に統一する（3.xは競合・AnyCPU禁止のため不可。pitfalls 4f参照） -->
    <PackageReference Include="Microsoft.Data.Sqlite" Version="10.0.11" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\nicorankLib\nicorankLib.csproj" />
  </ItemGroup>

  <ItemGroup>
    <None Include="config.json.org" />
    <None Include="cookie.txt.org" />
    <!-- 動画情報取得（NicoApi.UpdateTumbInfo）がConfig.ThreadMaxを読むため、nicorank.xmlを出力へ同梱する。
         oldlogが使う設定はスレッド数のみ。ビルド時点で複写されるため、しきい値変更時は再ビルドが必要（運用注意） -->
    <Content Include="..\依存ファイル\nicorank.xml">
      <Link>nicorank.xml</Link>
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </Content>
  </ItemGroup>

</Project>
