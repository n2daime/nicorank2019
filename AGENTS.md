# AGENTS.md

このリポジトリで作業する際のルール。あなたは日本語で応答すること。すべての出力、コードコメント、設計ドキュメント、タスク説明は日本語を使用する。**まず §0〜§2 の手順とゲートを守る。コンテキスト節約はそれに優先しない。**

## §0 絶対ルール(MUST)

以下は省略禁止。例外は「docs・調査(develop 直可)」「ユーザーの明示許可」「release.md のリリース手順(ユーザーのリリース指示時)」のみ。例外が免除するのはブランチ・Issue・レビューの要否に関する項目のみで、MUST 5〜7 の検証義務は免除しない。`main` は保護ブランチであり、例外時を除き直接コミット禁止。

### 着手前

1. コード変更タスクでは、最初に `git branch --show-current` を実行して結果を報告する。**この出力の前に実装ファイルを編集しない。**
2. feature ブランチ上でなければ、`git checkout develop` → `git pull` の後に `git checkout -b feature/tXXX-kebab-case-description` を作成してから着手する。`main` / `develop` への直接コミットは禁止(例外時のみ)。
3. 機能追加・改善では、新規 GitHub Issue を作成して番号を取得する。
4. `docs/tasks.md` の「未完了タスク」にタスクを追記する(対応する Issue 番号を関連付ける)。

### 完了まで

5. テスト + ビルドを成功させる(`$LASTEXITCODE = 0`)。実行方法は §5 を参照。
6. reviewer レビューを受けたら、実装(修正)に着手する前に指摘ごとの対応方針を一覧で報告する(下記フォーマット)。この報告を出力せずに修正を始めてはならない。必須(高・中深刻度)指摘は修正して**再レビュー**する(問題なしになるまで繰り返す)。任意(低深刻度)指摘を見送る場合は理由をレビュー依頼文とコミットメッセージに残す。全指摘への対応が完了したら、指摘ごとに実際どう対応したかのサマリーを報告する(下記フォーマット)。この報告を出力せずに次の工程(実行確認・マージ等)に進んではならない。

   ```
   reviewer指摘への対応方針:
   - [深刻度] ファイル:行 — 指摘内容の要約 → 対応(修正する / 見送る+理由)
   ```

   ```
   reviewer指摘への対応結果:
   - [深刻度] ファイル:行 — 実施内容(修正した差分の要約 / 見送った理由)
   ```
7. UI / 集計結果・出力内容などユーザーに見える挙動の変更は、ユーザーの実行確認を得る。
8. `git checkout develop` → `git merge --no-ff feature/tXXX-...` でマージし、feature ブランチを削除する。コンフリクトは解消してからマージし、解消不能ならユーザーに報告する。`develop` → `main` へのマージはユーザーの明示指示があるときのみ行う(リリース手順は `docs/knowledge/release.md` に従う)。プッシュはユーザーの指示があるときだけ行う。
9. マージ後、`develop` 上で tasks.md の完了更新・archive.md へのノウハウ追記・Issue クローズ・knowledge 最新化を行う。リリース時は `main` 上でも同様の更新を行う。

### 運用原則

10. 本ワークフローに従うコミット・ブランチ操作・マージは、ユーザーの事前承認済みとみなす。個別の許可を求めず、ワークフローに従って判断・実行してよい。
11. プロセス義務(Issue/tasks.md/knowledge/archive.md の読み書き、ゲートの出力)はコンテキスト節約の対象外。削ってよい作業ではない。
12. 判定基準: C# ソース(.cs / .csproj / .config 等)の変更 = コード変更(Issue+feature ブランチ必須)。README/AGENTS.md/docs のみは `develop` 直可(ブランチ不要)。ただし `main` 直は禁止(リリース時のみ)。判定やスコープに迷ったら、勝手に省略せずユーザーに確認する。
13. クローズ済み Issue の範囲内のバグはその Issue を ReOpen する。範囲外の新規不具合は新規 Issue を作る。判断に迷ったらユーザーに確認する。
14. プロセス規則が必要な作業(Issue 作成・ブランチ操作・レビュー依頼・マージ・docs 更新)はサブエージェントへ委譲しない(サブエージェントには AGENTS.md が渡らないため)。

## §1 作業開始ゲート

引き受けたら最初に種別を判定する。**次の出力をする前にコード変更を開始してはならない:**

```
種別: 機能追加 / バグ修正 / docs・調査
現在のブランチ: <git branch --show-current の結果>
使用ブランチ: feature/tXXX-...(docs・調査のみ「不要」)
Issue: #xxx(docs・調査は「なし」。バグ修正は対応 Issue。機能追加は新規 Issue 作成後に記入)
```

| 種別 | 必要なアクション |
|------|------------------|
| 機能追加・改善 | 新規 Issue → tasks.md 追記 → feature ブランチ(develop起点) → 実装 → developへマージ |
| バグ修正 | 対応 Issue を特定(クローズ済みなら ReOpen)→ feature ブランチ(develop起点) → 実装 → developへマージ |
| docs・調査 | ブランチ不要(develop 直可)。ソース変更を含む場合はコード変更扱い |

- 作業開始時に必ず `docs/knowledge/README.md` を読み、コード構造・依存関係を把握してから着手する。
- ブランチ名は小文字 + タスク ID + kebab-case の説明(例: `feature/t101-weekly-log-validation`)。大文字混在(`feature/T77-...`)は使わない。
- 複数タスクを並行する場合はタスクごとに独立した feature ブランチを切り、切替前に作業中の変更をコミットまたは `git stash` してから `git checkout` する。マージはタスク単位で行う。

### 実装中のプロセス制約

- 不具合修正は、同種問題の横展開(同じバグクラスが他箇所に無いか)を実装前に explore で調査する。含める範囲と見送る項目を決め、見送り理由はレビュー依頼文・コミットメッセージに残す。スコープ判断に迷ったらユーザーに確認する。
- 補正値・閾値・間隔等のマジックナンバーは定数化して調整しやすくする。ドキュメントには値を書かない。
- SQLite への大量登録を実装する前に、`docs/knowledge/pitfalls.md` のバッチ設計(5000件単位コミット + INSERT OR IGNORE + パラメータ再利用)を確認する。
- 仕様(挙動の定義)を変える変更は、specs.md / design.md への反映を実装計画に含める。

## §2 完了ゲート(チェックリストを自分で列挙して出力してから報告・マージする)

### マージ前(feature ブランチ上で)

- [ ] テスト + ビルド成功(EXIT CODE = 0)
- [ ] reviewer 指摘への対応方針を一覧報告済み(MUST 6 のフォーマット)
- [ ] reviewer レビュー済み、必須(高・中深刻度)指摘をすべて解消し再レビュー済み
- [ ] reviewer 指摘への対応結果サマリーを報告済み(MUST 6 のフォーマット)
- [ ] UI / 集計結果・出力内容などユーザーに見える変更はユーザーが実行確認済み

### マージ後(develop 上で)

- [ ] tasks.md を完了扱いに更新、関連 Issue に検証結果を追記してクローズ
- [ ] knowledge を最新化(コード構造・依存・設計判断が変わった場合)
- [ ] archive.md に検証履歴・実装ノウハウを追記

### リリース後(main 上で、ユーザー指示による develop→main 後)

- [ ] tasks.md / knowledge / archive.md が develop と同期されていることを確認
- [ ] タグ `vYYYYMMDD_<名前>` が main に付与されていることを確認

### レビュー依頼(テンプレート)

- レビュー対象(ブランチ・コミット範囲)/ 背景(この変更が何をするか)/ 確認観点(列挙)/ 返却形式(問題点: 深刻度 / ファイル:行番号 / 修正案、問題なしと判断した点、推奨対応)
- レビューの必要性に疑問がある場合、勝手に省略・実施せずユーザーに確認する。

## §3 コンテキスト節約

節約は価値だが、**§0〜§2 のプロセス義務には優先しない**(MUST 11)。

- 探索・検索・独立調査はサブエージェント(`explore` / `general`)へ委譲する。「何を調べるか」「何を返すか(最終出力の形式)」を明記し、結果だけを受け取る。
- 同じエージェントへの繰り返し依頼(再レビュー・再相談・追調査など)は、初回のセッションを `task_id` でレジュームして依頼する。背景・観点の再説明は省き、今回の差分・修正内容は毎回渡す。対象が大きく変わったら新規セッションで依頼する。
- 自分で Read/Grep を連発せず、1回の探索で済ませる。大きなファイルは全件読まず対象範囲を絞って読む。
- コードの実装はメインエージェントが直接行う(impl への委譲はしない。委譲用のタスク記述と結果検証でトークンが余計にかかるため)。委譲するのは「調査・計画・レビュー・画像解析」のみ。

## §4 ドキュメント方針(3層責任境界)

| 場所 | 責務 |
|------|------|
| AGENTS.md | プロセスルールのみ。技術的詳細は書かない |
| docs/specs.md / design.md | 仕様変更は specs.md、設計は design.md に反映 |
| docs/tasks.md | 現行タスクと「未完了タスク」のみ。要件・受け入れ条件は GitHub Issue がソース |
| docs/tasks/archive.md | 経緯・検証履歴。Issue に無い固有情報の置き場所 |
| docs/proposal.md | 変更提案のフローと過去の開発背景 |
| docs/knowledge/ | コードの現状態。追補を積まず書き直す。タスク番号を理由に使わない |

- ナレッジの最新化タイミングは §2 のマージ後ゲートで必ず実施する(後回し禁止)。
- 個別の技術的ピットフォール(SQLite 大量登録・ニコニコ API 403・csproj 形式等)は `docs/knowledge/pitfalls.md` に集約し、AGENTS.md 本体には置かない(詳細・更新ルールは `docs/knowledge/README.md`)。

## §5 ツール制約

### dotnet

- テスト・ビルドの長い出力をそのまま返すとエージェントの応答が遅延・停止することがある。全出力をファイルへリダイレクトし、`$LASTEXITCODE` を主判定にしてシェル側で判定を完結させる。成功時はログ本文を一切返さない。実行例:
  ```
  $log = "$env:TEMP\opencode\dotnet_log.txt"
  dotnet test UnitTest/UnitTest.csproj *> $log
  $code = $LASTEXITCODE
  if ($code -eq 0) { Write-Output "TEST_RESULT=SUCCESS" }
  else { Write-Output "TEST_RESULT=FAILED"; Get-Content $log -Tail 15 }
  Write-Output "EXIT_CODE=$code"
  ```
- テスト・ビルド後の応答は「成功/失敗 + EXIT CODE」の判定文だけにし、出力の再掲・考察をしない。
- テストと次のアクション(ビルド等)は 1 回の応答で連続して実行する(止まって再入力を待たせない)。

### その他

- ハング・中断されたコマンドを黙って再実行しない。ユーザーには進行が見えず無限ループにしか見えない。再実行する場合は必ず「前回どこで止まったか / 原因の推測 / 何を変えて再実行するか」を一文添えてから実行する。ハングが繰り返される場合はコマンド自体(デッドロック等)を疑って調査する。
- PowerShell 5.1 の `Set-Content` で UTF-8 ソースを書き換えない。BOM なし UTF-8 を ANSI(Shift-JIS ロケール)として読むため日本語コメントが破壊される。ファイル書き換えは edit ツールを使う。BOM 除去等のバイト操作は `[System.IO.File]::ReadAllBytes/WriteAllBytes` を使う(`-Encoding UTF8` は BOM を付けるため不可)。

## §6 エージェント委譲(pm / reviewer / image)

- pm(計画・設計): 相談は**任意**。ユーザーが全コードを把握しているため、設計判断・スコープ判断で悩んだらユーザーに確認するのが基本。pm を使うのは第二意見・手戻り防止の観点が欲しい場合(「やること・判断したい点・参考コード/Issue」を渡し返答形式を指定する)。相談結果は archive.md に残す。
- reviewer(レビュー): 実装後。手順・テンプレートは §2 参照。
- image(画像解析): 実行中モデルが画像を読めない場合、スクリーンショットの確認・計測(見た目・余白・位置の解析)を委譲する。画像を読めるモデルなら任意。

## §7 リリース

`main` は保護ブランチ。`develop → main` へのマージおよびタグ作成 `vYYYYMMDD_<名前>` → GitHub Release は、ユーザーの明示指示があるときのみ `docs/knowledge/release.md` の手順に従って行う。この場合に限り `main` への直接操作は MUST 2 の例外とする。日常の開発は `develop` までで完結させる。
